procs:
  minio:
    shell: "docker compose -f compose.dev.yml up minio 2>&1 | cat"

  inngest:
    cmd: ["npm", "run", "inngest:dev"]

  next:
    cmd: ["npm", "run", "dev"]

  prisma:
    cmd: ["npx", "prisma", "studio"]
